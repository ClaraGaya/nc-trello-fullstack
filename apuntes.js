lists: [
    0: {
        'name': '',
        'cards': [2,4]
    },
    {},
];
cards: {
    1: 'Buy milk',
    2:'Do laundry'
}

cards={list.cards.map(function(card_id){
    this.state.cards[cards_id]
)}

this.setState(function(state){
    return {
        lists:state.lists
    }
})